# Copy values into inventory.ini [media:vars] — Ansible templates these into .env on the server

# Server
server_domain=media.example.com
admin_email=admin@example.com
server_name=media-server
frontend_title=My Media Server
timezone=Europe/London

# Streaming profile enforced in Radarr/Sonarr so grabs stay playable on your
# client without server-side transcoding. Optional — defaults to chromecast-2018.
#   chromecast-2018 — H.264 <=1080p, no HEVC/DTS/TrueHD/4K/interlaced captures
#   none            — no restrictions (lifts previously-applied blocks)
# Change it and re-run with --tags init to switch profiles.
# streaming_profile=chromecast-2018

# Optional secrets — auto-generated into .credentials/ next to your inventory
# if omitted, so re-runs reuse the same value. Set them to choose your own.
# Seerr has no separate login: it reuses the Jellyfin username/password.
# qbittorrent_password=change-me
# jellyfin_password=change-me
# prowlarr_api_key=
# radarr_api_key=
# sonarr_api_key=

# jellyfin_api_key is auto-generated on first deploy (scripts/init-services.sh)
